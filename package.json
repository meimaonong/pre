{
  "devDependencies": {
    "prettier": "^1.14.2"
  },
  "scripts": {
    "fix": "eslint --fix \"src/**/*.js\"",
    "format": "prettier --single-quote --trailing-comma es5 --write \"src/**/*.js\" && yarn fix"
  }
}
